<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yd05.mprj.weather.service.WeatherMapper">
	<resultMap type="com.yd05.mprj.weather.service.WeatherVO" id="WeatherMap">

	</resultMap>
	
	
	<select id="weatherSelectSearchList" resultMap="WeatherMap">
                     <!-- 항상 true -->
         <if test="key == '전체'">1 = 1</if>
         <if test="key == '제목'">w.spotname LIKE '%' || #{val } || '%'</if>
   </select>

</mapper>